# The canonical origin of the deployed site, no trailing slash. THE one place to change
# when the custom domain is attached (or set SITE_URL in Vercel's project settings, which
# overrides this). Every canonical, og:url, sitemap entry and JSON-LD url is built from it.
# The default below is a reserved placeholder domain, not a real site.
SITE_URL=https://zemya.example
